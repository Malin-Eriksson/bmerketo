
@inject ProductRepo productRepo

@model ProductsIndexViewModel

@{
	ViewData["Title"] = "Products";

	@*var products = await productService.GetAllAsync();*@
}


<div>
	@foreach(var product in await productRepo.GetAllAsync())
	{
		<div>
			<div><a asp-action="details" asp-route-id="@product.Id">@product.Name</a></div>
			<div>
				@foreach(var tag in product.ProductTags)
				{
					<span>@tag.Tag.TagName</span>
				}
			</div>
			<div></div>
		</div>
	}
</div>


@*<div class="container mt-5">
	@foreach (var product in Model.Products)
	{
		<div class="card mb-3">
			<div class="card-body">
				<div class="d-flex justify-content-start align-items-center">
					<img src="~/images/products/@product.ImageUrl" alt="@product.Name" height="50" width="50" />
					<div class="ms-2">
						<div>@product.ArticleNumber</div>
						<div>@product.Name</div>
					</div>
				</div>
			</div>
		</div>		
	}
</div>*@








@*@model ProductsIndexViewModel

<div style="padding-top: 150px;" >

	<partial name="/Views/Partials/_GridCollection.cshtml" model="@Model.All" />

</div>*@

